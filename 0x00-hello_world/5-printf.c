#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point 
 *
 * return: always (0) success
 */
int main(void)
/*a program that prints a string to the output*/
	{
		printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
	}
