#include "main.h"

/**
 * main - Entry point
 * Prints alphabet in lowercase
 * Return: Always 0 (success)
*/

int main(void)
	{
		char c;

		for(c = 'a'; c <= 'z'; c++)
		{
			putchar(c);
		}

		return 0;
	}

